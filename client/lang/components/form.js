export const en = {
    submitButtonDefault: 'Submit',
    invalid: {
        default: '{label} invalid',
        required: '{label} required',
        minLength: 'Minimum {minLength} characters required',
        exactLength: 'Exactly {exactLength} characters required',
        exactMatch: '{label} must match {matchLabel}',
        regex: 'Provide correct format for {label}',
    },
}
